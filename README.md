# From DMRs to Disease Diagnosis: A Causal and Inferential Framework for Schizophrenia Using DNA Methylation

This repository contains the code and derived data used for the study **"From DMRs to Disease Diagnosis: A Causal and Inferential Framework for Schizophrenia Using DNA Methylation."**

The project implements a hierarchical analytical framework integrating DNA methylation analysis, differential methylation region (DMR) identification, statistical feature selection, machine learning, SHAP-based feature selection, and hierarchical causal discovery for schizophrenia classification.

---

## Repository Structure

```text
.
├── code/
│   ├── preprocessing/
│   ├── baseline_construction/
│   ├── classification/
│   ├── causal_discovery/
│   ├── analysis/
│   └── figures/
│
├── data/
│   ├── methylation/
│   ├── metadata/
│   ├── eaa/
│   ├── classification/
│   └── ...
│
├── results/
│   ├── classification_performance/
│   ├── causal_discovery/
│   ├── statistical_analysis/
│   └── ...
│
├── figures/
│   ├── main/
│   └── supplementary/
│
├── requirements.txt
├── main.py
└── README.md
```

### `code/`

Contains the Python source code implementing the analytical workflow.

Each major analytical stage contains its own driver and supporting modules. Detailed documentation for individual stages is provided in the corresponding directories.

The figure-generation scripts are maintained separately from the main analytical pipeline because they generate publication figures from previously generated results rather than producing inputs required by subsequent analytical stages.

### `data/`

Contains the input and derived datasets used throughout the analysis.

The directory is organized according to the stage at which datasets are introduced or generated.

### `results/`

Contains computational outputs generated by the analytical pipeline, including trained models, feature-selection results, classification performance, causal-discovery results, and statistical analysis results.

### `figures/`

Contains publication-ready figures generated from the analysis results.

```text
figures/
├── main/
└── supplementary/
```

---

# Analysis Overview

The overall workflow follows a hierarchical progression from DNA methylation measurements to DMRs, genes, functional modules, and causal relationships.

```text
DNA methylation
       │
       ▼
      CpGs
       │
       ▼
      DMRs
       │
       ├──────────────► Statistical feature selection
       │
       └──────────────► SHAP-based feature selection
                              │
                              ▼
                         Classification
                              │
                              ▼
                     Performance evaluation

DMRs
 │
 ▼
Gene mapping
 │
 ▼
WGCNA modules
 │
 ▼
Module-level causal discovery
 │
 ▼
Gene-level causal discovery
 │
 ▼
DMR-level causal discovery
```

The major analytical stages are:

1. DNA methylation preprocessing and quality control
2. DMR identification
3. DMR variance filtering
4. DMR correlation-network pruning
5. Statistical baseline construction
6. SHAP-based baseline construction
7. Classification model training
8. Classification performance evaluation
9. Bootstrap-based model comparison
10. WGCNA-based module construction
11. Module-level causal discovery
12. Gene-level causal discovery
13. DMR-level causal discovery
14. Statistical and biological interpretation
15. Publication figure generation

---

# Data

The analysis uses DNA methylation data together with sample metadata and epigenetic age acceleration measurements.

The methylation datasets are represented using sample-level DMR matrices. Metadata and epigenetic age acceleration features are subsequently joined using `sample_id`.

The principal datasets used in the analysis include:

- GEO GSE80417
- GEO GSE84727
- GEO GSE147221
- GEO GSE152027

The methylation measurements are based on the Illumina HumanMethylation450 BeadChip platform.

Where redistribution of the original datasets is restricted, the repository provides derived data and processing outputs where permitted rather than redistributing restricted source data.

---

# Preprocessing

The preprocessing stage performs quality control and preparation of the DNA methylation data for downstream DMR analysis.

The preprocessing workflow includes removal of:

- Sex-chromosome probes
- Multi-mapping probes
- Cross-reactive probes
- Other probes failing the predefined quality-control criteria

Beta values are transformed to M-values for downstream statistical analysis.

Batch effects are corrected using NeuroComBat.

---

# DMR Analysis

Differentially methylated regions are identified from the processed methylation data.

DMR detection is performed using **DMRcate** together with the corresponding linear-model framework.

Statistical significance is controlled using the Benjamini-Hochberg procedure with:

```text
FDR < 0.05
```

The resulting DMR matrix forms the basis for subsequent feature-selection and classification analyses.

---

# Baseline Construction

The baseline-construction stage generates the statistical and SHAP-derived feature sets used for comparison with the final causal feature set.

The classification baselines are constructed from DMR features together with the appropriate metadata and epigenetic age acceleration covariates.

The baseline construction workflow includes:

1. DMR variance filtering
2. DMR correlation-network pruning
3. Statistical baseline construction
4. Top-DMR selection by variance
5. Logistic regression training
6. Random forest training
7. SHAP computation
8. SHAP feature ranking
9. SHAP-based feature selection
10. Construction of the final SHAP-derived datasets

Detailed documentation is provided in:

```text
code/baseline_construction/
```

---

# SHAP-Based Feature Selection

SHAP values are computed independently for the logistic regression and random forest models.

For each model, features are ranked according to their mean absolute SHAP contribution.

The cumulative relative SHAP contribution is then used to select the features accounting for the predefined proportion of the total SHAP contribution.

The resulting feature sets are model-specific.

The SHAP-derived datasets are subsequently used for classification-performance comparison.

---

# Classification

The classification stage evaluates the different feature sets using two machine-learning models:

- Logistic Regression
- Random Forest

Both models are trained using the training portion of each dataset.

Hyperparameter optimization is performed using:

```text
5-fold StratifiedKFold cross-validation
```

with:

```text
shuffle = True
random_state = 42
```

ROC-AUC is used as the model-selection metric during hyperparameter optimization.

### Logistic Regression

The logistic regression pipeline consists of:

```text
StandardScaler
      │
      ▼
LogisticRegression
```

Class imbalance is addressed using:

```text
class_weight = "balanced"
```

### Random Forest

The random forest classifier also uses:

```text
class_weight = "balanced"
```

and parallel processing through all available workers.

---

# Classification Feature Sets

The classification analysis evaluates several feature configurations.

The principal dataset identifiers are:

| Dataset | Description |
|---|---|
| `C0` | Statistical baseline |
| `C1` | Final causal/integrated feature set |
| `S0` | SHAP-derived baseline |
| `S1` | Model-specific SHAP-derived feature set |

Because SHAP selection is performed independently for each classification model, the `S1` datasets are represented internally as:

```text
s1_lr
s1_rf
```

where:

- `s1_lr` is the SHAP-selected dataset associated with logistic regression
- `s1_rf` is the SHAP-selected dataset associated with random forest

For reporting purposes, both are referred to as `S1`.

---

# Classification Evaluation

Classification performance is evaluated on the corresponding held-out test dataset for each trained model.

The evaluation metrics are:

- Accuracy
- Precision
- Recall
- F1 score
- F2 score
- ROC-AUC

Each model is evaluated only on the test dataset corresponding to its training feature configuration.

For example:

```text
train_c0.csv
      │
      ├── Logistic Regression
      │
      └── Random Forest
                │
                ▼
           test_c0.csv
```

For the model-specific SHAP datasets:

```text
train_s1_lr.csv
      │
      ▼
Logistic Regression
      │
      ▼
test_s1_lr.csv
```

and:

```text
train_s1_rf.csv
      │
      ▼
Random Forest
      │
      ▼
test_s1_rf.csv
```

Trained models are saved under:

```text
results/classification_performance/models/
```

Classification-performance results are saved under:

```text
results/classification_performance/performance_metrics/
```

---

# Bootstrap Metric Comparison

Pairwise classification-performance comparisons are performed using bootstrap resampling.

The comparisons evaluate:

```text
C0 vs C1
C1 vs S0
C1 vs S1
```

for both logistic regression and random forest.

The comparisons are performed using paired bootstrap samples of the same test observations.

For each comparison and metric, the bootstrap distribution of:

```text
Metric(dataset 2) - Metric(dataset 1)
```

is calculated.

The 95% confidence interval is obtained from the:

```text
2.5th percentile
97.5th percentile
```

of the bootstrap distribution.

The resulting comparison table contains:

```text
dataset_comparison_pair
model
accuracy_ci
precision_ci
recall_ci
f1_score_ci
f2_score_ci
roc_auc_ci
```

Bootstrap distributions can additionally be saved as publication figures where applicable.

---

# Gene Mapping

Significant DMRs are mapped to genes using the predefined transcription-start-site region.

The mapping region used in the analysis is:

```text
TSS -2000 bp to +500 bp
```

The resulting DMR-to-gene relationships are used for subsequent gene-level and module-level analyses.

---

# WGCNA

Weighted Gene Co-expression Network Analysis (WGCNA) is used to identify groups of co-expressed genes.

The network construction uses a soft-thresholding power selected using the scale-free topology criterion.

The analysis uses:

```text
Scale-free topology R² ≥ 0.9
```

with a minimum module size of:

```text
30 genes
```

The resulting module eigengenes are used as inputs to module-level causal discovery.

---

# Causal Discovery

Causal discovery is performed hierarchically.

## Module-Level Causal Discovery

Module eigengenes are first analyzed to identify relationships between WGCNA modules and disease status.

The resulting causal structure is used to identify modules with potential direct relationships with disease.

## Gene-Level Causal Discovery

Genes associated with the selected modules are mapped back into a gene-level representation.

Feature reduction is performed before causal discovery using the predefined filtering and ranking procedures.

The resulting gene-level causal structure is used to identify candidate disease-associated genes.

## DMR-Level Causal Discovery

Genes identified at the gene level are mapped back to their associated DMRs.

Causal discovery is then performed at the DMR level to identify candidate methylation regions with direct relationships to disease.

---

# Causal Feature Selection

The hierarchical causal analysis progressively reduces the feature space:

```text
Module level
     │
     ▼
Selected modules
     │
     ▼
Gene mapping
     │
     ▼
Selected genes
     │
     ▼
DMR mapping
     │
     ▼
Selected DMRs
```

The final causal feature set is incorporated into the `C1` classification dataset.

---

# Statistical Analysis

Statistical analyses are performed using the predefined statistical models and correction procedures described in the corresponding analysis modules.

Where multiple hypothesis testing is performed, the Benjamini-Hochberg false discovery rate procedure is used according to the analysis-specific significance criteria.

Detailed implementation information is available in the relevant analysis directories.

---

# Figures

Publication figures are generated separately from the primary analysis pipeline.

The figure-generation scripts use previously generated datasets and results and therefore do not form part of the main computational dependency chain.

Figures are organized as:

```text
figures/
├── main/
└── supplementary/
```

The corresponding figure-generation code is maintained under:

```text
code/figures/
```

This separation allows the analytical pipeline to be executed independently of the publication-specific visualization scripts.

---

# Reproducibility

The project uses a pinned Python dependency specification.

Install the required dependencies using:

```bash
pip install -r requirements.txt
```

The complete analytical pipeline can be executed using:

```bash
python main.py
```

Individual stages can also be executed independently using the stage-specific driver scripts documented in their respective README files.

The recommended execution order follows the analytical workflow described above.

---

# Main Pipeline

The root-level pipeline driver is:

```text
main.py
```

It coordinates the main analytical stages.

Figure-generation scripts are intentionally excluded from `main.py` because they are downstream visualization tasks rather than components of the main analytical pipeline.

---

# Project Outputs

The main computational outputs are organized under:

```text
results/
```

These include:

```text
results/
├── classification_performance/
│   ├── models/
│   └── performance_metrics/
│
├── causal_discovery/
│
├── statistical_analysis/
│
└── ...
```

Publication figures are stored separately under:

```text
figures/
```

This separation distinguishes computational results from their visual representations.

---

# Software and Dependencies

The project is implemented in Python.

Major dependencies include:

- NumPy
- pandas
- NetworkX
- scikit-learn
- SHAP
- DoWhy
- LiNGAM
- statsmodels
- joblib
- gProfiler
- neuroCombat
- pyaging

The exact versions used for the analysis are specified in:

```text
requirements.txt
```

---

# Reproducibility and Random Seeds

Where applicable, deterministic random seeds are specified in the analysis code.

The primary seed used throughout the machine-learning and bootstrap procedures is:

```text
42
```

This includes model initialization, cross-validation shuffling, and bootstrap sampling where applicable.

---

# Data Availability

The repository contains the data products required for reproducing the documented downstream analyses where redistribution is permitted.

Original public datasets are identified by their corresponding GEO accession numbers:

```text
GSE80417
GSE84727
GSE147221
GSE152027
```

Users requiring the original source datasets should obtain them from the corresponding public repositories when redistribution is restricted.

---